# ── Supabase ─────────────────────────────────
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ── Google OAuth (configure in Supabase Dashboard) ──
# No frontend env vars needed. Setup steps:
#   1. Supabase Dashboard > Authentication > Providers > Google > Enable
#   2. Create OAuth credentials at https://console.cloud.google.com/apis/credentials
#   3. Set redirect URI in Google Console: https://<project-ref>.supabase.co/auth/v1/callback
#   4. Paste Client ID & Secret into Supabase Google provider settings
#   5. Add your site URL under Authentication > URL Configuration > Site URL

# ── Stripe ───────────────────────────────────
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-key

# ── App ──────────────────────────────────────
VITE_APP_URL=http://localhost:5173
VITE_APP_NAME=The Aira Edit
